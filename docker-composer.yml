version: '3.9'

services:
  app: 
    build: .
    ports:
      - "3000:4000"
    volumes:
      - .:app/
      - app/node_modules
    depends_on:
      - mongo
    
  mongo : 
    image: mongo:6.0
    ports:
      - "27017:27017"
    volumes:
      - mongo-data:/data/db
volumes:
  mongo-data:
